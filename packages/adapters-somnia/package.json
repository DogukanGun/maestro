{
  "name": "@agentraft/adapters-somnia",
  "version": "0.1.0",
  "description": "AgentRaft adapters: Somnia EVM (ConsensusGate, IdentityProvider, event-anchored LogStore).",
  "license": "MIT",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist", "contracts/*.sol", "README.md"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "hardhat:compile": "hardhat compile",
    "hardhat:test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.ts --network shannon"
  },
  "dependencies": {
    "@agentraft/core": "workspace:*",
    "ethers": "^6.13.4"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@nomicfoundation/hardhat-ethers": "^3.0.8",
    "@openzeppelin/contracts": "^5.0.2",
    "@types/node": "^22.9.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.16",
    "typescript": "^5.6.3"
  }
}
