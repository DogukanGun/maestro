{
  "name": "agentraft",
  "version": "0.1.0",
  "private": true,
  "description": "Fault-tolerant multi-agent protocol with Raft-like consensus. Framework-agnostic core + 0G adapters.",
  "license": "MIT",
  "packageManager": "pnpm@10.12.4",
  "scripts": {
    "build": "pnpm -r --filter './packages/*' build",
    "test": "pnpm -r test",
    "test:core": "pnpm --filter @agentraft/core test",
    "lint:core-deps": "node scripts/check-core-deps.mjs",
    "swarm": "pnpm --filter treasury-demo swarm"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
